using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Testastos : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Debug.Log("ID : " + Scenes.Id + "\nNom : " + Scenes.Nom + "\nAnime : " + Scenes.Anime);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
